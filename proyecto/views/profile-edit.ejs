<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link width="" rel="shortcut icon" href="./images/logo-mercado-liebre.jpg" />

	<title> Mercado Liebre Argentina</title>
	<!-- Font Awesome Stylesheet -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
	<!-- Bootstrap Grid Stylesheet -->
	<link rel="stylesheet" href="./css/bootstrap-grid.min.css">
	<!-- Own Stylesheet -->
	<link rel="stylesheet" href="./css/app.css">
</head>

<body>
	<!-- Header -->
	<% if (locals.user != undefined) {%>
		<%- include('partials/headerLogueado') %>

	<% } else { %>
		<%- include('partials/header') %>

	<% } %>
	<!-- /Header -->

	<!-- Novedades -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Editar mi perfil</h2>
			</div>
			<!-- Show the "ultimos" products -->
			<h2 class="col-12"> 
				<!-- <i>///////////////////////////////////////// <br> Acá va el formulario de edición de datos del perfil de usuario. <br>
			Debe cumplir con los puntos de la consigna y ser coherente con la tabla de la base de datos deñ usuario<br>
			Eliminen estás líneas y coloquen el formulario. <br>
			Recuerden todo lo que necesita tanto la etiqueta form como los campos input para operar correctamtente</i> <br>
			//////////////// -->
		
			<% if (locals.errores) { %>
				<p><%=  locals.message %></p>
			<% } %>

			<section class="form-register">
				<form action="/login/edit/<%= user.id%>" method="post" >
					<input  type="email" name="email" id="nombres" value="<%= user.email %>" >
					<input  type="text" name="usuario" id="nombres" value="<%= user.usuario %>">
					<input  type="password" name="contrasenaAnterior" id="nombres" placeholder="Ingrese su contraseña anterior">
					<input type="password" name="contrasena" id="nombres" placeholder="Ingrese su nueva contraseña ">
					<input type="date" name="fecha" id="nombres" placeholder="Ingrese su fecha de nacimiento">
					<input  type="number" name="documento" id="nombres" value="<%= user.dni %>" placeholder="DNI">
					<input  type="text" name="imagen" id="nombres" value="<%= user.foto %>" placeholder="FOTO">
					<input  type="hidden" name="id" id="nombres" value ="<%= user.id %>">

					<button type="submit">Guardar informacion</button>
				</form>
			</section>

	<!-- footer -->
	<%- include('partials/footer') %>
	<!-- /footer -->
</body>

</html>